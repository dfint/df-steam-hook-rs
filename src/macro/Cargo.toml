[package]
name = "macro"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
quote = "1.0.20"
syn = { version = "*", features = [
  "full",
  "parsing",
  "printing",
  "proc-macro",
] }
